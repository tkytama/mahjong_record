<div class="devise-wrapper">
  <div class="devise-comment">
    <h2>ユーザー情報の編集</h2>
  </div>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email",size: :"30" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.label :name %> <br />
      <%= f.text_field :name,size: :"30" %>
    </div>

    <div class="field">
      <i> 新しい</i><%= f.label :password %> <em>最低<%= @minimum_password_length %>桁</em>
      <br><em>※変更しない場合は空白</em><br />
      <%= f.password_field :password, autocomplete: "new-password",size: :"30" %>
      <% if @minimum_password_length %>
        <br />
      <% end %>
    </div>

    <div class="field">
      <i>新しい</i><%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password",size: :"30" %>
    </div>

    <div class="field">
      <%= f.label :current_password %> <br />
      <%= f.password_field :current_password, autocomplete: "current-password",size: :"30" %>
    </div>

    <div class="field">
      <%= f.label :image %>
      <%= f.file_field :image, class: 'form-control' %>
    </div>

    <div class="white-button">
      <%= f.submit "変更" %>
    </div>
  <% end %>
  <div class="devise-note">
    <h3>アカウントの削除</h3>

    <p>アカウントの削除を行うと、<br />これまでに登録したデータが全て削除されます。 <%= button_to "アカウントの削除", registration_path(resource_name), data: { confirm: "本当にアカウントを削除しますか？" }, method: :delete %></p>
  </div>
  <%= link_to "もどる", :back %>
</div>
